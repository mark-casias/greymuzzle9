{#
/**
 * @file
 * Theme override for status messages.
 *
 * Displays status, error, and warning messages, grouped by type.
 *
 * An invisible heading identifies the messages for assistive technology.
 * Sighted users see a colored box. See http://www.w3.org/TR/WCAG-TECHS/H69.html
 * for info.
 *
 * Add an ARIA label to the contentinfo area so that assistive technology
 * user agents will better describe this landmark.
 *
 * Available variables:
 * - type: one of 'error', 'alert' or 'status',
 * - message: text of the status block.
 * - attributes: HTML attributes for the element, including:
 *   - class: HTML classes.
 */
#}
{% set status__base_class = 'status' %}
{% if type %}
<div aria-label="{{ type }}"{{ attributes|without('aria-label') }}>
  {% if type == 'error' %}
    <div role="alert">
  {% endif %}
    {% if type %}
      <h2 class="visually-hidden">{{ type }}</h2>
    {% endif %}
    <ul {{ bem('list', [], status__base_class)}}>
      <li {{ bem(status__base_class, [type])}}>{{ message }}</li>
    </ul>
  {% if type == 'error' %}
    </div>
  {% endif %}
</div>
{% endif %}
